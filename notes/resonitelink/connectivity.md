# ResoniteLink connectivity

## Preferred endpoints

- Prefer direct ResoniteLink WebSocket endpoints when proxying is on hold.
- Docker host alias can be reachable at `ws://host.docker.internal:40864/` (verified locally).
